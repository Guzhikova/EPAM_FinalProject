@using DentalOffice.WebUI.Management;
@{
    Layout = "~/Pages/Layouts/_MainLayout.cshtml";

    string status = Request["status"];

}

 @if(status == "reg-success")
    {
    <div class="alert alert-success text-center m-4" role="alert" id="status-mess">
        <strong>@User.Identity.Name</strong>, поздравляем с успешной регистрацией на нашем сайте!
    </div>
    }
